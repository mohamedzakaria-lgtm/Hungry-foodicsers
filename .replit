# Replit configuration file

language = "nodejs"
run = "node server.js"
entrypoint = "server.js"

[deploy]
run = ["sh", "-c", "node server.js"]

[env]
PORT = "5000"

[ports]
localPort = 5000
externalPort = 80
